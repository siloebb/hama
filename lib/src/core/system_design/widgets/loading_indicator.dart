import 'package:flutter/material.dart';

class HAMLoadingIndicator extends StatelessWidget {
  const HAMLoadingIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return CircularProgressIndicator();
  }
}